### Node.js project based on workshop offer by http://howtocode.com.br

##### Defining alias to test with mocha
    $ npm install
    $ npm install -g mocha
    $ mongod --dbpath=tmp

##### Running Tests
    $ ./test
